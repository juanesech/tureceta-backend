- name: build
  service: gradle
  command: ./gradlew clean build

- name: test
  service: gradle
  command: java -jar codacy-coverage-reporter-4.0.1-assembly.jar report -l Java -r $JACOCO_REPORT -t $CODACY_PROJECT_TOKEN